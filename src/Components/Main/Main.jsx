import React from "react";
import './Main.css';
import { GeneratorSettings } from "./GeneratorSettings/GeneratorSettings";

export const Main = () => {
    return (
        <main>
            <GeneratorSettings />
        </main>
    );
}